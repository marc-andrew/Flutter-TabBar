import 'package:flutter/material.dart';

class TabPageTwo extends StatelessWidget {
  @override
  Widget build(BuildContext context){
    return new Container(
      child: new Center(
        child: new Icon(Icons.cloud_off, size: 120.0, color: Colors.black87)
      )
    );
  }
}